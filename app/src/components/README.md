# Components Documentation

## Overview

This directory contains all React components for the Wealth Tracker application, including main UI components, form components, and reusable UI elements. The components are organized into main components and a UI subdirectory for shared elements.

## Files

### Navigation.tsx
**Purpose**: Main navigation component providing responsive navigation with authentication integration.

**Key Components**:
- Responsive navigation bar with desktop and mobile layouts
- Clerk authentication integration (SignIn, SignUp, UserButton)
- Mobile menu toggle functionality with smooth transitions

### UserProfileForm.tsx
**Purpose**: Form component for collecting and editing user profile information with validation.

**Key Components**:
- Form fields for personal details (name, address, dates)
- Real-time validation and error display
- Responsive grid layout for optimal mobile and desktop experience

### DocumentUpload.tsx
**Purpose**: Document upload component with drag-and-drop functionality for financial documents.

**Key Components**:
- Drag and drop interface with visual feedback
- File validation (PDF, PNG, JPG, JPEG up to 20MB)
- Multiple file upload support with progress indication
- Error handling and user feedback

### AnimatedHero.tsx
**Purpose**: Animated hero section component for landing page with dynamic visual effects.

### DashboardSidebar.tsx
**Purpose**: Sidebar navigation component for dashboard with menu items and user profile section.

### DocumentList.tsx
**Purpose**: Component for displaying and managing uploaded documents with actions.

### ExtractionReview.tsx
**Purpose**: Review component for extracted financial data with editing capabilities.

### HeroChart.tsx
**Purpose**: Chart component for displaying financial data visualizations in hero section.

### NetWorthSummary.tsx
**Purpose**: Summary component showing net worth calculations and financial overview.

### StatementForm.tsx
**Purpose**: Form component for adding financial statements with validation.

### StatementList.tsx
**Purpose**: List component for displaying financial statements with management options.

## UI Components (ui/ subdirectory)

### button.tsx
**Purpose**: Reusable button component with multiple variants and sizes using Base UI.

**Key Components**:
- Multiple variants (default, outline, secondary, ghost, destructive, link)
- Various sizes (xs, sm, default, lg, icon variants)
- Loading state and disabled handling
- Class-variance-authority for consistent styling

### card.tsx
**Purpose**: Card container component for content grouping with consistent styling.

### dialog.tsx
**Purpose**: Modal dialog component for overlays and confirmations.

### input.tsx
**Purpose**: Input field component with validation states and styling.

### label.tsx
**Purpose**: Label component for form inputs with accessibility features.

### select.tsx
**Purpose**: Select dropdown component for form inputs.

### table.tsx
**Purpose**: Table component for displaying structured data.

### separator.tsx
**Purpose**: Visual separator component for content division.

### icon-badge.tsx
**Purpose**: Badge component with icon support for status indicators.

### GradientBackground.tsx
**Purpose**: Background component with gradient effects for visual appeal.

### InsightCard.tsx
**Purpose**: Card component for displaying financial insights and metrics.

### NetWorthCard.tsx
**Purpose**: Specialized card for net worth display with formatting.

### PricingCard.tsx
**Purpose**: Card component for pricing plans and subscription options.

### SectionContainer.tsx
**Purpose**: Container component for consistent section layout and spacing.

---

*This documentation was auto-generated by the build-code-dir-metadata workflow*
